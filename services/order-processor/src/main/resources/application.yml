# Order Processor configuration
# Running alongside .NET Aspire services

spring:
  application:
    name: order-processor

  datasource:
    url: jdbc:postgresql://localhost:5432/orderingdb
    username: postgres
    password: 71UhdH_{f7C+yPyrh92RRW
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    addresses: localhost:5672
    username: guest
    password: WBpzyj95KTuVkpGxR5Fx1j

  boot:
    admin:
      client:
        url: http://localhost:9090
        instance:
          service-url: http://host.docker.internal:9105

server:
  port: 9105
  address: 0.0.0.0
  max-http-request-header-size: 48KB

background-task:
  grace-period-time: 1
  check-update-time: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,loggers
  endpoint:
    health:
      show-details: always
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces

logging:
  level:
    com.eshop: DEBUG
